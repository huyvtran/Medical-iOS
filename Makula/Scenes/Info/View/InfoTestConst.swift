extension Const {
	/// Consts for testing the scene.
	struct InfoTests {
		/// Accessibility identifier for views.
		struct ViewName {
			/// The scene's main view.
			static let mainView = "InfoView"
			/// The text view to show instructions.
			static let textView = mainView + "TextView"
		}
	}
}
