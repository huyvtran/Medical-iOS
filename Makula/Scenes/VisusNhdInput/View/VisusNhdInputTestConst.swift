extension Const {
	/// Consts for testing the scene.
	struct VisusNhdInputTests {
		/// Accessibility identifier for views.
		struct ViewName {
			/// The scene's main view.
			static let mainView = "VisusNhdInputView"
			/// The table view.
			static let tableView = mainView + "TableView"
			/// The confirm button.
			static let confirmButton = mainView + "ConfirmButton"
		}
	}
}
