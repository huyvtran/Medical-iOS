import UIKit

extension Const {
	/// Any constants for this scene.
	struct Diagnosis {
		/// All view margins.
		struct Margin {
			/// The margin for the cells.
			static let cell = Const.Margin.default.top(8).bottom(5)
		}
	}
}
