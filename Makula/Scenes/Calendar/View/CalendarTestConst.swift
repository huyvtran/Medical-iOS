extension Const {
	/// Consts for testing the scene.
	struct CalendarTests {
		/// Accessibility identifier for views.
		struct ViewName {
			/// The scene's main view.
			static let mainView = "CalendarView"
			/// The week names view.
			static let weekNamesView = mainView + "WeekNamesView"
		}
	}
}
