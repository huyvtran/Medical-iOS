import UIKit

extension Const {
	/// Any constants for this scene.
	struct Splash {
		struct Time {
			/// The duration time in seconds the logo performs its fade-in animation.
			static let logoFadeInDuration: TimeInterval = 0.25
			/// The delay in seconds before automatically transitioning.
			static let transitionDelay: TimeInterval = 2.0
		}
	}
}
