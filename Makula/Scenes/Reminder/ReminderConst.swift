import UIKit

extension Const {
	/// Any constants for this scene.
	struct Reminder {
		/// The interval of minutes to show in the picker, e.g. every 5 minutes.
		static let pickerMinuteInterval = 5
		/// The maximum of hours to display in the picker.
		static let pickerMaxHours = 23
	}
}
