import UIKit

extension Const {
	/// Any constants for this scene.
	struct AppointmentDatePicker {
		struct Value {
			/// The interval for the date picker in minutes.
			static let pickerMinuteInterval = 5
			/// The gap size between the navigation view and the picker in the content.
			static let pickerGap: CGFloat = 44
		}
	}
}
