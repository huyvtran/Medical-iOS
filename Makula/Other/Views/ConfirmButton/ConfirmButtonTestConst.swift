extension Const {
	/// Test constants for the view.
	struct ConfirmButtonTests {
		/// Accessibility identifier for views.
		struct ViewName {
			/// The button view.
			static let buttonView = "ConfirmButton"
			/// The title label.
			static let titleLabel = buttonView + "TitleLabel"
			/// The arrow icon image.
			static let arrowIcon = buttonView + "ArrowIcon"
		}
	}
}
