extension Const {
	/// Test constants for the view.
	struct TextButtonTests {
		/// Accessibility identifier for views.
		struct ViewName {
			/// The button view.
			static let buttonView = "TextButton"
			/// The normal label.
			static let label = buttonView + "Label"
			/// The separator view.
			static let separatorView = buttonView + "SeparatorView"
		}
	}
}
