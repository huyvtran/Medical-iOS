extension Const {
	/// Test constants for the view.
	struct ImageButtonTests {
		/// Accessibility identifier for views.
		struct ViewName {
			/// The button view.
			static let buttonView = "ImageButton"
			/// The normal image view.
			static let normalImageView = buttonView + "NormalImageView"
			/// The highlighted image view.
			static let highlightImageView = buttonView + "HighlightImageView"
		}
	}
}
