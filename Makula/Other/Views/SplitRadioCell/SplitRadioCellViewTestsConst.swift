extension Const {
	/// Test contants for the view.
	struct SplitRadioCellViewTests {
		/// Accessibility identifier for views under test.
		struct ViewName {
			/// The `SplitRadioCellView`.
			static let splitRadioCellView = "SplitRadioCellView"
			/// The title label.
			static let titleLabel = splitRadioCellView + "TitleLabel"
			/// The left button.
			static let leftRadioButton = splitRadioCellView + "LeftRadioButton"
			/// The right radio button.
			static let rightRadioButton = splitRadioCellView + "RightRadioButton"
		}
	}
}
