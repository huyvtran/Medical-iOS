import UIKit

extension Const {
	struct SplitCellView {
		struct Size {
			/// The gap for a title label to the center.
			static let middleGap: CGFloat = 10.0
			/// The gap between the vertical separator and the top.
			static let verticalSeparatorUpperGap: CGFloat = 21.0
			/// The gap between the vertical separator and the bottom.
			static let verticalSeparatorLowerGap: CGFloat = 19.0
		}
	}
}
