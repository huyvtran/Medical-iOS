extension Const {
	/// Test constants for the view.
	struct CheckmarkButtonTests {
		/// Accessibility identifier for views.
		struct ViewName {
			/// The button view.
			static let buttonView = "CheckmarkButton"
			/// The circle image view.
			static let circleImageView = buttonView + "CircleImageView"
			/// The checkmark image view.
			static let checkmarkImageView = buttonView + "CheckmarkImageView"
		}
	}
}
