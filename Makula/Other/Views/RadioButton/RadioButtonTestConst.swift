extension Const {
	/// Test constants for the view.
	struct RadioButtonTests {
		/// Accessibility identifier for views.
		struct ViewName {
			/// The button view.
			static let buttonView = "RadioButton"
			/// The circle image view.
			static let circleImageView = buttonView + "CircleImageView"
			/// The radio image view.
			static let radioImageView = buttonView + "RadioImageView"
		}
	}
}
