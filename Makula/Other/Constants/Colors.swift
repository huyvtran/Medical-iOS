import SwiftHEXColors
import UIKit

extension Const {
	/// Any specific app colors if they need to be created in code rather than imported via a palette.
	struct Color {
		static let pompadour: UIColor = UIColor(hex: 0x610F4F)!
		static let gray: UIColor = UIColor(hex: 0xA8A8A9)!
		static let disabled: UIColor = UIColor(hex: 0x515151)!
		static let darkMain: UIColor = UIColor(hex: 0x000029)!
		static let yellow: UIColor = UIColor(hex: 0xFEBE09)!
		static let green: UIColor = UIColor(hex: 0x19C104)!
		static let magenta: UIColor = UIColor(hex: 0xFF00FF)!
		static let white: UIColor = UIColor(hex: 0xFFFFFF)!
		static let lightMain: UIColor = UIColor(hex: 0x2983FF)!
	}
}
